<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/contact_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="40dp"
        android:text="@string/contact" />

    <Button
        android:id="@+id/courses_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/contact_button"
        android:layout_below="@+id/contact_button"
        android:layout_marginTop="20dp"
        android:text="@string/courses_taught" />

    <Button
        android:id="@+id/events_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/courses_button"
        android:layout_below="@+id/courses_button"
        android:layout_marginTop="20dp"
        android:text="@string/events" />

    <Button
        android:id="@+id/news_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/events_button"
        android:layout_below="@+id/events_button"
        android:layout_marginTop="20dp"
        android:text="@string/news" />

</RelativeLayout>
